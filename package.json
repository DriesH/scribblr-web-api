{
  "private": true,
  "scripts": {
    "install-all": "cp env.example env && npm install && cd ./bootstrap && mkdir cache && cd ../ && composer update && php artisan key:generate"
  },
  "devDependencies": {
    "axios": "^0.15.3",
    "bootstrap-sass": "^3.3.7",
    "concurrently": "^3.4.0",
    "cross-env": "^3.2.3",
    "jquery": "^3.1.1",
    "laravel-mix": "^0.8.3",
    "lodash": "^4.17.4",
    "mkdirp": "^0.5.1"
  }
}
